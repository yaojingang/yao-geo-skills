# Examples

This repository package keeps only lightweight fixtures by default. Real Doubao Android/Appium runs can contain screenshots, XML, raw logs, and account-specific UI evidence, so they should remain in local `runs/` directories unless explicitly prepared for public release.

Use `fixtures/sample-doubao-crawl.json` and `fixtures/sample-doubao-mobile-crawl.json` for offline report verification.
