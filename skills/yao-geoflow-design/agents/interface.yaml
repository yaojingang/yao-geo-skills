interface:
  display_name: "Yao GEOFlow Design"
  short_description: "Discover, preview, and edit GEOFlow themes safely"
  default_prompt: "Use $yao-geoflow-design to discover GEOFlow themes, create a preview edit session for a selected theme, or clone a reference style into a GEOFlow-compatible theme."
compatibility:
  canonical_format: "agent-skills"
  adapter_targets:
    - "openai"
    - "claude"
    - "generic"
  activation:
    mode: "manual"
    paths: []
  execution:
    context: "inline"
    shell: "bash"
  trust:
    source_tier: "local"
    remote_inline_execution: "forbid"
    remote_metadata_policy: "allow-metadata-only"
  degradation:
    openai: "metadata-adapter"
    claude: "neutral-source-plus-adapter"
    generic: "neutral-source"
